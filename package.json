{
  "name": "update-pr-issues",
  "version": "1.0.0",
  "description": "GitHub Action that updates PR description with referenced issue numbers from commits",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "jest": "^29.7.0"
  },
  "license": "MIT"
}

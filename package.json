{
  "name": "update-pr-issues",
  "version": "1.0.1",
  "description": "GitHub Action that updates PR description with referenced issue numbers from commits",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --target=node22 --format=cjs --outfile=dist/index.cjs",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "jest": "^29.7.0"
  },
  "jest": {
    "transform": {}
  },
  "license": "MIT"
}